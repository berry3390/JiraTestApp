name: CI

on:
  push:
    branches:
      - master
      - develop
      - 'develop/**'
  pull_request:
    branches:
      - master
      - develop

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Extract version name
      run: |
        echo "start------"
        echo ${{ steps.release.outputs.new-version }}
    - name: get appbundle
      with:
        name: appbundle
        path: ./app/build.gradle
        run: |
          echo "bundle version1 : '${{versionName}}'"
          echo "bundle version2 : '${{android.defaultConfig.versionName}}'"
      
      
        
        
